export default defineAppConfig({
    ui: {
        primary: 'violet',
        gray: 'slate',
        input: {
            default: {
                color: 'gray',
            }
        },
        select: {
            default: {
                color: 'gray',
            }
        }
    }
});